// MenuSection is no longer used as an accordion.
// MenuPage renders items directly in the content panel.
// This file is kept as a no-op re-export to avoid broken imports if anything still references it.
export default function MenuSection() { return null }
