# Leave blank in development — Vite proxies /api → http://localhost:4000 automatically.
# In production set this to your deployed API base URL (no trailing slash).
# VITE_API_URL=https://api.your-domain.com/api
